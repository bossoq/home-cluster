apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: mosquitto
  name: mosquitto
spec:
  type: NodePort
  selector:
    app: mosquitto
  ports:
  - port: 30000
    nodePort: 1883
    targetPort: 1883
    protocol: TCP
    name: default
  - port: 30001
    nodePort: 9001
    targetPort: 9001
    protocol: TCP
    name: websocket

apiVersion: v1
kind: PersistentVolume
metadata:
  name: zigbee2mqtt-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 100Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data/zigbee2mqtt"
